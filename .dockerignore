# Keep the build context small + the image clean.
# Anchored (leading slash) so they can't match src/codec_avatars/data/ etc.
.git
.gitignore
/runs/
/outputs/
/data/
/.multiface_repo/
**/__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
*.pt
*.npy
*.egg-info/
